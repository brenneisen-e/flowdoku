# Cloudflare Pages Configuration
name = "dex-documentation"
compatibility_date = "2024-01-01"

pages_build_output_dir = "dex-event-app/dist"

[build]
command = "cd dex-event-app && npm install && npm run build"
